version: "3.8"

networks:
  task_manager_app_network:
    external: true

services:
  node_app:
    image: node:latest
    container_name: node_app
    working_dir: /app
    command: sh -c "npm run tsc"
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
    volumes:
      - ./:/app
    networks:
      - task_manager_app_network
